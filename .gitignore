# ── Build output ────────────────────────────────────────────────────────────
.gradle/
build/
*.class

# ── IDE ─────────────────────────────────────────────────────────────────────
.idea/

# ── Hyperskill infrastructure (root-level only; task/build.gradle is kept) ──
/build.gradle
/settings.gradle

# ── All YAML / YML config and metadata ──────────────────────────────────────
*.yaml
*.yml

# ── Gradle wrapper binaries ──────────────────────────────────────────────────
gradlew
gradlew.bat
gradle/

# ── Hyperskill task HTML descriptions ────────────────────────────────────────
**/*.html

# ── H2 database files ────────────────────────────────────────────────────────
*.mv.db
*.trace.db

# ── Claude Code tooling ──────────────────────────────────────────────────────
.claude/
